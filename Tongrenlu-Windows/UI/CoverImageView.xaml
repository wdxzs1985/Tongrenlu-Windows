<UserControl x:Class="Tongrenlu_Windows.UI.CoverImageView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Tongrenlu_Windows.UI"
             mc:Ignorable="d" 
             d:DesignHeight="160" d:DesignWidth="160">
    <Grid Background="{StaticResource Primary}">
        <Image Name="CoverImage"
            HorizontalAlignment="Center" 
            VerticalAlignment="Center"
            Grid.Column="0"></Image>

        <TextBlock Name="Title" 
                   Text="{Binding title, FallbackValue=Title}" Grid.Column="1"
                   Foreground="{StaticResource PrimaryText}"
                   TextWrapping="Wrap"
                   Background="{StaticResource LightDisabledText}"
                   Padding="4" FontSize="24"></TextBlock>
    </Grid>
</UserControl>
